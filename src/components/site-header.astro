---
import HorizontalMenu from "./horizontal-menu.astro";
import DropdownMenu from "./dropdown-menu.astro";

const siteLinks = [
  { name: "Home", href: "/" },
  { name: "Projects", href: "/projects" },
  { name: "Arcade", href: "/arcade" },
  { name: "Blog", href: "/blog" },
  { name: "Now", href: "/now" },
];

const currentPage = Astro.url.pathname;
---

<header
  class="relative z-10 container mx-auto mt-6 mb-12 flex max-w-2xl items-center gap-3 rounded-xl p-3 md:border md:border-stone-800 md:bg-stone-800/40">
  <HorizontalMenu siteLinks={siteLinks} currentPage={currentPage} />
  <DropdownMenu siteLinks={siteLinks} />
</header>
