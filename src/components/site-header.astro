---
import HorizontalMenu from "./horizontal-menu.astro";
import DropdownMenu from "./dropdown-menu.astro";

const siteLinks = [
  { name: "Home", href: "/" },
  { name: "Projects", href: "/projects" },
  { name: "Arcade", href: "/arcade" },
  { name: "Blog", href: "/blog" },
  { name: "Now", href: "/now" },
];

const currentPage = Astro.url.pathname;
---

<header
  class="relative z-10 container mx-auto flex max-w-2xl items-center gap-3 rounded-xl p-3 md:mt-6 md:mb-12 md:border md:border-stone-800/60 md:bg-stone-800/30">
  <HorizontalMenu siteLinks={siteLinks} currentPage={currentPage} />
  <DropdownMenu siteLinks={siteLinks} />
</header>
