---
import ActionButton from "./action-button.astro";
import ProfilePic from "./profile-pic.astro";
import ExtLink from "./ext-link.astro";
import { about, interest } from "../content/home/home-content";
---

<div class="hidden lg:block"><ProfilePic /></div>
<div class="space-y-3 text-sm font-medium text-stone-100">
  <div class="flex items-center gap-1.5">
    <span class="size-4 rounded-full bg-white"></span>
    <span>San Francisco Bay Area, CA</span>
  </div>
  <button class="flex cursor-pointer items-center gap-1.5">
    <span class="size-4 rounded-full bg-white"></span>
    <span>Available For v</span>
  </button>
  <button class="flex cursor-pointer items-center gap-1.5">
    <span class="size-4 rounded-full bg-white"></span>
    <span>Share Profile</span>
  </button>
</div>

<div>
  <h3 class="mb-1.5 text-sm font-medium text-stone-500">{about.header}</h3>
  <p class="text-sm font-medium text-stone-300">{about.content}</p>
</div>

<div>
  <h2 class="mb-1.5 text-sm font-medium text-stone-500">{interest.header}</h2>
  <p class="text-sm font-medium text-stone-300">
    Besides programming, I also enjoy
    <ExtLink
      href={interest.running.href}
      title={interest.running.title}
      ariaLabel={interest.running.title}>{interest.running.text}</ExtLink
    >, <ExtLink
      href={interest.hiking.href}
      title={interest.hiking.title}
      ariaLabel={interest.hiking.title}>{interest.hiking.text}</ExtLink
    >, traveling,
    <ExtLink
      href={interest.pcGaming.href}
      title={interest.pcGaming.title}
      ariaLabel={interest.pcGaming.title}>{interest.pcGaming.text}</ExtLink
    >, <ExtLink
      href={interest.pcBuilding.href}
      title={interest.pcBuilding.title}
      ariaLabel={interest.pcBuilding.title}>{interest.pcBuilding.text}</ExtLink
    >, cooking, and watching movies and anime.
  </p>
</div>

<ActionButton>My Resume</ActionButton>
