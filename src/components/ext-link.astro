---
const { href, target, rel, title, ariaLabel } = Astro.props;
---

<a
  href={href}
  target={target}
  rel={rel}
  title={title}
  aria-label={ariaLabel}
  class="text-white hover:text-orange-600 hover:underline">
  <slot />
</a>
